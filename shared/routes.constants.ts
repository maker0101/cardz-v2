export const PAGE_ROUTES = {
  Home: '/',
  Study: '/study',
  Library: '/library',
  Progress: '/progress',
  Settings: '/settings',
} as const;

export const API_ROUTES = {
  Cards: {
    Generate: '/api/cards/generate',
  },
} as const;
